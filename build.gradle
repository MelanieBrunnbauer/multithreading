plugins {
    id 'java'
}

group 'mb.learning'
version '1.0-SNAPSHOT'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
    testCompile group: 'org.mapdb', name: 'thread-weaver', version: '3.0.mapdb'
}
